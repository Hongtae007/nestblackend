// import { SetMetadata } from '@nestjs/common';
// import { USERTYPE } from 'src/users/enum/users.enum';

// export const UserTypes = (...userTypes: USERTYPE[]) =>
//   SetMetadata('userTypes', userTypes);
